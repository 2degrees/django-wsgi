**************************************************
twod.wsgi -- WSGI as first-class citizen in Django
**************************************************

This package improves Django's limited WSGI support by implementing patches proposed
for Django, but which never received enough interest, as well as many other
features to make WSGI a first-class citizen in Django applications.
